const cover=document.getElementById('cover'),seal=document.getElementById('seal'),invite=document.getElementById('invite'),music=document.getElementById('music'),musicToggle=document.getElementById('musicToggle');
const MAX=.72,FADE=2.4;let raf=null,restarting=false;
function cancelFade(){if(raf)cancelAnimationFrame(raf);raf=null}function fadeTo(target,ms,done){cancelFade();const from=music.volume,start=performance.now();const step=n=>{const t=Math.min(1,(n-start)/ms);music.volume=from+(target-from)*t;if(t<1)raf=requestAnimationFrame(step);else{raf=null;done&&done()}};raf=requestAnimationFrame(step)}
async function startSoft(){restarting=false;music.currentTime=0;music.volume=0;try{await music.play();fadeTo(MAX,1100)}catch(e){}}
seal.addEventListener('click',async()=>{await startSoft();invite.classList.add('show');invite.setAttribute('aria-hidden','false');cover.classList.add('open');setTimeout(()=>{cover.style.display='none';document.body.classList.remove('locked');musicToggle.classList.add('show');scrollTo(0,0)},1550)},{once:true});
music.addEventListener('timeupdate',()=>{if(!music.duration||restarting||music.paused)return;const remain=music.duration-music.currentTime;if(remain<=FADE){restarting=true;fadeTo(0,Math.max(300,remain*1000),async()=>{music.currentTime=0;music.volume=0;try{await music.play();fadeTo(MAX,1100);restarting=false}catch(e){restarting=false}})}});music.addEventListener('ended',startSoft);
musicToggle.addEventListener('click',async()=>{if(music.paused){music.volume=0;try{await music.play();fadeTo(MAX,700)}catch(e){}musicToggle.classList.remove('muted');musicToggle.textContent='♪'}else{fadeTo(0,400,()=>music.pause());musicToggle.classList.add('muted');musicToggle.textContent='×'}});
const target=new Date('2026-09-12T20:00:00+03:00').getTime();function tick(){let l=Math.max(0,target-Date.now()),d=Math.floor(l/86400000);l%=86400000;let h=Math.floor(l/3600000);l%=3600000;let m=Math.floor(l/60000);l%=60000;let s=Math.floor(l/1000);days.textContent=String(d).padStart(2,'0');hours.textContent=String(h).padStart(2,'0');minutes.textContent=String(m).padStart(2,'0');seconds.textContent=String(s).padStart(2,'0')}tick();setInterval(tick,1000);
